import { withAuth } from "next-auth/middleware";

export default withAuth({
  pages: {
    signIn: "/login",
  },
});

export const config = {
  matcher: [
    "/dashboard/:path*",
    "/resume-builder/:path*",
    "/ats-checker/:path*",
    "/analyze-resume/:path*",
    "/cover-letter/:path*",
    "/career-roadmap/:path*",
    "/interview-prep/:path*",
  ],
};